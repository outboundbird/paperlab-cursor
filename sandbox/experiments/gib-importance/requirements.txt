# Python dependencies for the gib-importance experiment.
#
# Install (in your active Python 3.10+ environment):
#     pip install -r sandbox/experiments/gib-importance/requirements.txt
#
# The vault module at <vault>/GIBGAT/code/method.py is loaded at runtime
# via importlib (see methods/vault_import.py); it depends only on torch.

# Core tensor / autograd. Used by both the vault method.py and the
# experiment's extended GIBGAT layer.
torch>=2.1,<3

# Graph data containers (Data, Batch). The experiment uses
# torch_geometric only for batching and Data objects; the GIBGAT
# layer is implemented manually, not via PyG GATConv.
torch_geometric>=2.4

# Synthetic data generation and array ops in the generator.
numpy>=1.24

# Recovery metrics: roc_auc_score and f1_score.
scikit-learn>=1.3
